#pragma once

#include <cstddef>

inline constexpr const char* PROJECT = "@PROJECT@";
inline constexpr int WIDTH = @WIDTH@;
inline constexpr int HEIGHT = @HEIGHT@;
inline constexpr float ASPECT_RATIO = float(WIDTH) / float(HEIGHT);

inline constexpr float PERSPECTIVE_FOV = @PERSPECTIVE_FOV@;
inline constexpr float PERSPECTIVE_NEAR = @PERSPECTIVE_NEAR@;
inline constexpr float PERSPECTIVE_FAR = @PERSPECTIVE_FAR@;

inline constexpr int NUM_SECTORS_CIRCLE = @NUM_SECTORS_CIRCLE@;
inline constexpr int NUM_SECTORS_SPHERE = @NUM_SECTORS_SPHERE@;
inline constexpr int NUM_STACKS_SPHERE = @NUM_STACKS_SPHERE@;

inline constexpr std::size_t MAX_ENTITIES = @MAX_ENTITIES@;

inline constexpr double G = @G@;

inline constexpr float MOUSE_SENS = @MOUSE_SENS@;
